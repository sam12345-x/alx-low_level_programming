#include <stdio.h>
int main(void)
{
    char ch = 'a';
    for (ch = 'a'; ch <= 'z'; ch++)
        putchar(ch);
    char c=' ';
     for (ch = ' '; ch <= ' '; ch++)
        putchar(ch);
    return (0);
}
