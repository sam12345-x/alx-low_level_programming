#include <stdio.h>
int main(void)
{
    int ch;
    for (ch = 'a'; ch <= 'z'; ch++)
        putchar(ch);<br>
    return (0);
   int ch;
    for (ch = 'A'; ch <= 'Z'; ch++)
        putchar(ch);
    return (0);
}
